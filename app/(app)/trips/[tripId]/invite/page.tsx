export default function InvitePage({ params }: { params: { tripId: string } }) {
  return (
    <div className="min-h-screen bg-background flex items-center justify-center">
      <p className="text-muted-foreground">Invite coming soon</p>
    </div>
  )
}
